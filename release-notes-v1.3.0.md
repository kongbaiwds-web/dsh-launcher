## 新增

- **自动启动 DSH 服务**：启动器启动时自动检测 DSH 服务（默认 `http://127.0.0.1:3080`）是否可访问；服务未运行时自动以隐藏窗口拉起服务，等待就绪后自动加载页面。重启电脑后双击启动器即可直接使用，无需再手动运行 `pnpm dsh web`
- **⚙ 菜单新增「启动 DSH 服务」**：手动拉起/恢复服务（服务已在运行则无操作）
- **服务配置可查看**：`HKCU\Software\DSHLauncher` 下新增 `ServerNode` / `ServerDir` / `ServerArgs` 三个注册表值，可查看 Node.js 路径、DSH 仓库目录和启动参数

## 改进

- 离线横幅的「重试」按钮现在会先检测并自动拉起服务，而不是只重新加载页面
- README 补充了自动启动服务的说明与日志位置

## 修复

- 重启电脑后 DSH 服务未运行时，启动器只显示「无法连接」横幅，需要手动到命令行启动服务；现在启动器会自动拉起服务并等待就绪后再加载页面
