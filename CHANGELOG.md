# 更新日志

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。


## [1.5.1] - 2026-08-21

### 修复

- **适配新版 DeepSeek Harness 默认自动打开浏览器**：新版 `dsh web` 默认会调用系统浏览器打开 Web UI，启动器现在会自动传入 `--no-open`，避免额外弹出浏览器
- **兼容旧版 Harness**：启动器会检测 Harness 是否支持 `--no-open`，新版自动追加，旧版不传，避免旧版因未知参数启动失败

## [1.5.0] - 2026-08-16

### 新增

- **一键安装程序（`DSHLauncherSetup.exe`）**：单文件安装程序（内嵌全部文件、黑色小鲸鱼图标），支持双击安装与命令行 / AI Agent 静默安装（`--silent` / `--autostart` / `--launch` / `--dir=路径`，退出码 0 = 成功）
- **⚙ 菜单新增「检查更新」**：自动查询 GitHub 上版本号最高的 Release 并与本地比较，有新版本时提示并跳转下载页（只提示、不自动更新）

### 改进

- **桌面图标修复**：修复 v1.4.0 图标生成矩阵变换 bug（鲸鱼被挤出画布导致图标空白），改为透明背景、居中、加粗描边的黑色小鲸鱼；同步修复构建脚本
- **README 完善**：功能列表补充 v1.4.0 内容；新增详细「安装」章节（下载安装 / 命令行一键安装 / 可选参数）
- **发版脚本升级**：发布时自动构建安装程序并作为 Release 附件上传（安装程序 + 便携版 exe 双附件）

### 修复

- 图标生成居中 bug（桌面图标空白）
- 安装程序解压兼容 Compress-Archive 反斜杠目录项（此前会导致安装失败）


## [1.4.0] - 2026-08-14

### 新增

- **DeepSeek Harness 图标**：窗口、托盘、桌面快捷方式统一使用黑色小鲸鱼图标；exe 文件图标升级为 256×256 高清版
- **项目显示名称更新**：窗口标题、托盘、关于等显示为 `DeepSeek Harness启动器`
- **桌面快捷方式自动迁移**：启动/构建时自动把旧 `DSH 启动器.lnk` 更新为 `DeepSeek Harness启动器.lnk`
- **⚙ 菜单新增「关闭 DSH 服务」**：一键结束正在监听 `127.0.0.1:3080` 的 DSH 服务进程
- **构建脚本增强**：自动生成图标、自动结束旧进程、自动更新桌面快捷方式


## [1.3.0] - 2026-08-14

### 新增

- **自动启动 DSH 服务**：启动器启动时自动检测 DSH 服务（默认 `http://127.0.0.1:3080`）
  是否可访问；服务未运行时，自动以隐藏窗口拉起服务，等就绪后自动加载页面。
  重启电脑后双击启动器即可直接用，**无需再手动在命令行运行 `pnpm dsh web`**。
  - 启动命令优先使用用户 PATH 上的 `pnpm.cmd dsh web`；PATH 里没有 pnpm 时自动回退到
    `node.exe --import tsx/esm apps/cli/src/bin.ts web`（即 `pnpm dsh web` 展开后的
    实际命令，使用绝对路径，开机自启等无终端场景也可靠）
  - 服务输出写入 `%LOCALAPPDATA%\DSHLauncher\dsh-server.log`，启动失败时方便排查
- **⚙ 菜单新增「启动 DSH 服务」**：手动拉起/恢复服务（服务已在运行则无操作）
- **服务配置可覆盖**：`HKCU\Software\DSHLauncher` 下新增 `ServerNode` / `ServerDir` /
  `ServerArgs` 三个注册表值，可覆盖 Node.js 路径、DSH 仓库目录和启动参数
  （默认值适配本机安装，一般无需改动）

### 改进

- 离线横幅的「重试」按钮现在会先检测并自动拉起服务，而不是只重新加载页面
- README 补充了自动启动服务的说明与日志位置

### 修复

- 重启电脑后 DSH 服务未运行时，启动器只显示「无法连接」横幅，需要手动到命令行启动
  服务；现在启动器会自动拉起服务并等待就绪后再加载页面

## [1.2.1] - 2026-08-14

### 修复

- 最小化按钮改为普通任务栏最小化，不再收进系统托盘（托盘仅用于关闭方式选择
  「最小化到托盘」时）

## [1.0.0] - 2026-08-13

### 新增

- WebView2 套壳的 DeepSeek Harness Web GUI 桌面启动器，包含：工具栏（后退/前进/刷新/
  主页/地址栏/外部浏览器）、离线横幅、系统托盘、单实例、窗口状态记忆、开机自启、
  桌面快捷方式、关闭方式可选（每次询问/最小化到托盘/直接退出）、地址自定义
